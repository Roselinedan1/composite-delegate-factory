[project]
name = "composite-delegate-factory"
authors = ["Blockchain Solutions Team"]
description = "Decentralized delegate management and factory contract"
telemetry = false
requirements = []
[contracts.delegate-factory]
path = "contracts/delegate-factory.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
